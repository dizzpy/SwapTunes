name: Deploy — Dev

on:
  push:
    branches: [dev]
    paths:
      - "backend/**"
  workflow_dispatch:

jobs:
  backend-lint:
    name: Gate — Backend Lint & Format
    uses: ./.github/workflows/_backend-ci.yml
  # Railway auto-deploys from dev branch after this passes
