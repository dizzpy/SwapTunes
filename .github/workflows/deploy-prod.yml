name: Deploy — Production

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
  workflow_dispatch:

jobs:
  backend-lint:
    name: Gate — Backend Lint & Format
    uses: ./.github/workflows/_backend-ci.yml
  # Railway auto-deploys from main branch after this passes
