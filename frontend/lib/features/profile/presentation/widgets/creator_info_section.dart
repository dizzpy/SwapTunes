import 'package:flutter/material.dart';

/// Creator-specific section shown only when userType == 'creator'.
///
/// Displays role title, location, specialization chips, and social link icons.
class CreatorInfoSection extends StatelessWidget {
  const CreatorInfoSection({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement with CreatorProfile data
    return const SizedBox.shrink();
  }
}
